use_default_settings: true

server:
  secret_key: "rnd_searxng_secret_2026_x7k9"
  bind_address: "0.0.0.0"
  port: 8080
  base_url: false
  image_proxy: false
  real_ip_header: "X-Forwarded-For"
  real_ip_proxies: ["0.0.0.0/0"]

outgoing:
  request_timeout: 3.0       # ৩ সেকেন্ডের বেশি লাগলে engine skip
  max_request_timeout: 6.0   # সর্বোচ্চ ৬ সেকেন্ড
  pool_connections: 100
  pool_maxsize: 20

ui:
  static_use_hash: true

search:
  safe_search: 0
  autocomplete: ""
  default_lang: ""
  formats: [html, json]

enabled_plugins:
  - 'Hash plugin'
  - 'Search on category select'
  - 'Self Informations'
  - 'Tracker URL remover'
